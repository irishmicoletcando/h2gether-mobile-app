<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="20dp"
        android:color="@color/azure" />
    <corners android:radius="360dp" /> <!-- Adjust the radius as needed -->
</shape>